{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Launch Frontend Dev Server",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder:frontend}"
    }
  ]
}

